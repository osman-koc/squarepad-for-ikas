import type { AlignOption, FormatOption } from '@/types/squarepad';

export const ALIGN_OPTIONS: AlignOption[] = ['center', 'top', 'bottom', 'left', 'right'];

export const FORMAT_OPTIONS: FormatOption[] = ['auto', 'jpeg', 'png', 'webp', 'avif'];
